// This file is intentionally left blank for now.
// We will build the dashboard layout in subsequent steps.
export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
